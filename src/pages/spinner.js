import React from 'react'
import PropTypes from 'prop-types'

const Spinner = props => {
  return (
    <div >
      <h1>Loading.</h1>
    </div>
  )
}

Spinner.propTypes = {}

export default Spinner
